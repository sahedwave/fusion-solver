[pytest]
markers =
    benchmark: optional benchmark/report-generation checks that regular CI may skip
    heavy: opt-in larger multigroup regression and benchmark checks
    ci_drift: deterministic software-regression checks intended for routine CI drift detection
    external_validation: reserved marker for future external benchmark/experimental validation suites
